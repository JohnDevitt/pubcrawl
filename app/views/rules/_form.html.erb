<%= form_for(@rule) do |f| %>
  <% if @rule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rule.errors.count, "error") %> prohibited this rule from being saved:</h2>

      <ul>
      <% @rule.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.text_field :title, class: "form-control", placeholder: "Give your rule a name" %>
  </div>
  <div class="form-group">
    <%= f.text_area :details, class: "form-control", placeholder: "You can be a bit more detailed about your rule here" %>
  </div>
  <div class="form-group">
    <%= f.submit "Add Rule", class: "btn btn-primary" %>
  </div>
<% end %>
